---
description: Start or continue AIWF planning and workflow routing
agent: aiwf-planner
subtask: false
---

Load the `aiwf-planner` skill. Run `aiwf status --prompt`, then handle this request.
At a Git or governance boundary, consult `aiwf governance status`; use the
governance checkpoint command only when the Planner flow or the user calls for
committing pending stable `.aiwf` files. Do not guess governance commands.

$ARGUMENTS
