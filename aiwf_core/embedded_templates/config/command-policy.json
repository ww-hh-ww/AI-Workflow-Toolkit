{
  "schema_version": 1,
  "description": "Human-only AIWF commands. Human terminal and TUI actions are not affected.",
  "deny": [
    {
      "command": "aiwf task force-close",
      "reason": "force-close bypasses all gates on the active task. Human emergency override only.",
      "human_only": true
    },
    {
      "command": "aiwf task interrupt",
      "reason": "interrupt releases the active task execution window. Human interruption only.",
      "human_only": true
    },
    {
      "command": "aiwf fixloop continue",
      "reason": "continue authorizes another workflow step after repeated failures. Human decision only.",
      "human_only": true
    }
  ]
}
