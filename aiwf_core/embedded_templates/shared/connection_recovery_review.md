## Connection Recovery

If interrupted before completing review, return `PAUSED_FOR_PLANNER` with: checked files, evidence reviewed, testing reviewed, partial findings, remaining review obligations, and whether it is safe to re-dispatch reviewer.

Do not record `accepted` unless the review actually completed.
